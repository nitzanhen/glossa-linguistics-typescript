/**
 * Represent an error caused by passing an incorrect value to a typescript enum
 * or a pseudo-enum class.
 *
 * @since 10/4/20
 */
class IllegalEnumValueError extends RangeError { }

export default IllegalEnumValueError;
