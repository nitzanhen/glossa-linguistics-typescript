import VerbFormProperties from "../VerbFormProperties";
import FiniteKeyProperties from "./FiniteKeyProperties";

//interface FiniteFormProperties extends VerbFormProperties, FiniteKeyProperties {}
/* class FiniteFormProperties {
  constructor(properties: VerbFormProperties & FiniteKeyProperties) {
    Object.assign(this, properties)
  }
} */

export default FiniteFormProperties;
